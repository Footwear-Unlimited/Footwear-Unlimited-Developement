<div class="banner__media media">
  {%- if desktop_image != blank -%}
    <div class="banner__media media {% if mobile_image != blank %}medium-hide small-hide{% endif %}">
      {% render 'image',
        image: desktop_image,
        image_max_width: 2800,
        image_desired_width: desktop_image.width,
        style: "max-width: 100%; height: 100%;"
        %}
    </div>
  {%- endif -%}

  {%- if mobile_image != blank -%}
    <div class="banner__media media large-up-hide">
      {% render 'image',
        image: mobile_image,
        image_max_width: 1100,
        image_desired_width: mobile_image.width,
        style: "max-width: 100%; height: 100%;"
        %}
    </div>
  {%- endif -%}
</div>